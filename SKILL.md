---
name: spec-flow
description: 复杂活的两段流程：方案期六步（描述需求 → 确认需求 → 出 spec → 过闸 → 修订合并 → issue 锚 spec 链接）+ 实施期三步（认领 issue → 实施 → 过闸合并回填），两段共用一道合并闸。当用户描述一个要设计、要动多个端、或值得留可回溯依据的需求时，开工前先调本 skill；认领 issue 做实施、「请你做 #N」这类只做实施的任务也走本 skill——从段二进，实施 PR 合并前同样过闸（⛔ agent 不得自派审核，每份交付物合并前都把档位摆给用户点）。另有「应用到项目」功能——用户说「把 spec-flow 应用到（本）项目 / 应用 spec-flow」（可带附加描述，如「同时启用 ADR」）时，一句话把目标项目切成就地指向 spec-flow 的只指向式采纳形态。
---

# 复杂活的两段流程

方案期六步 + 实施期三步，两段**共用一道合并闸**。

按顺序走，**别跳步、别把两步合成一步**（每份交付物合并前都各过一道闸，见下面闸节）：

**段一·方案期**（新需求从这里进）

1. **用户描述需求**
2. **一起确认需求** —— 有岔路就问清，别猜；能自己查能自己判的先做完再问
3. **用户点头之后**才出 spec —— 这是**硬关口**：需求没确认就不动 spec 文件
4. **过闸** —— spec PR 合并前（见闸节）
5. **按需要修订 → 开 PR 并合并**
6. **开 issue** —— spec PR 合并之后开，正文锚 spec 的永久链接

**段二·实施期**（认领 issue 从这里进）

7. **认领 issue** —— 锁 = worktree 分支名 `issue-<N>`（细则见下「认领与防撞车」）
8. **实施** —— 在 worktree 里干活（规矩见「工作区规矩」节）
9. **过闸 → 合并 → 回填** —— 实施 PR 合并前过闸（见闸节）；合并后回填 issue、关单、放锁

**闸**独立一节、两段共用——档位怎么摆、审什么、什么免过，两段同一套。

## 各步细则·段一（方案期）

**第 2/3 步（确认需求 / 出 spec）——两张清单，方案期和 spec 期都管：**

- **机制清单**：方案 / spec 每新增一个机制、概念或抽象，单列一条，每条一行写清「解决什么 / 没有它会怎样」。判定口径：用户事后可能问「这是什么 / 哪来的」的，都算。没有新增就写「无新增机制」——清单不许空着不表态。呈现时配卡「全要 / 全砍 / 逐项挑」——机制不许混在方案正文里让用户批量点头。**spec 期这份清单写进 spec 文件**（方案期随方案摆卡即可）——只活在对话里审核就拿不到，下一条核对成了空转。
- **自定细节清单**：spec 没写死、agent 自己定的细节（默认值、判定规则、命名、落点），逐条列在 spec 末尾「自定细节」节，每条一行。用户扫一眼、不逐项问；该列不列 = 违规。
- spec 期审核对应加一条核对项：**两张清单都在**——机制清单在 spec 里、自定细节节在末尾（它后面只剩「审核修订记录」不算违规；缺任一 = 违规）；再逐条对，spec 里的机制 ↔ 机制清单对不上、清单外有机制 = 漏报。

**第 4 步（过闸）——spec PR 合并前：**

摆卡、档位、审什么、什么免过，全在闸节，两段同一套。这里只标 spec 期这道闸的语境：**审的就是 spec 本身**，审完才合并、才开 issue。

**第 5 步（修订 → 合并）——⛔ 先读这条硬规矩（不管有没有做审核，这条都照走）：**

**改一条「判据 / 定义 / 常量 / 术语」时，不是改完那一句就完了——它一定在别处有回响。**
**必须全文搜它的所有落点，逐一同步：**

| 落点 | 是什么 |
|---|---|
| 机制 / 原理节 | 「怎么判、为什么这么判」的正文 |
| 实现 / 数据源节 | 用哪个键、哪份产物、哪个脚本 |
| 实施步骤 | 要不要新建脚本/组件、谁做 |
| 验收标准 | 有没有对应的可验项（**没有 = 这条判据没进验收，等于没落**） |
| **跨文档引用** | 父文档、**「唯一 owner」文档**、兄弟 spec |
| 历史记录节 | 旧裁决与新裁决打架时，旧的要标「已被取代」 |

⚠️ **最容易漏的三个**：① **父文档**（改了子 spec、忘了几百行外的总纲）；② **唯一 owner 文档**（改了成员/口径、忘了那个自称 owner 的）；③ **落地物**（spec 里写了、脚本/代码里其实没有——这种"文档有、实物无"要单独标出来）。

⚠️ **改表格**：markdown 单元格**不支持换行**（要 `<br>`）——把长内容塞进单元格会把整张表写断。改完**数一遍每行未转义的管道数**，与表头不一致的就是写坏了。

⚠️ **改章节编号**：先 `grep` 谁引了这个号（代码注释、别的文档）——编号一动，锚点全废。

**为什么有这条**（2026-09-11 反爬专项的实例）：把 IDC 判据从「CIDR 段」扩成「CIDR ∪ ASN」，
**只改了判据那一句** —— 机制、实现、实施步骤、验收四处全没动，父文档总纲 §0 也没改。
**照当时的 spec 施工会做成 CIDR-only，等于白改。**独立审核抓出 2 处严重。

---

- 只含 spec / 文档的 PR 合并到主干是**长期授权**，不用每次问（**审核档位过闸**，见闸节——这条授权管的是「合并动作要不要再问」，**不管闸**）
- ⚠️ 代码 PR 不在此授权内，仍按「Git: 危险操作」的规矩走

**第 6 步（issue）：**
- ⏰ **时机：spec PR 合并之后、代码实施之前**（owner 2026-09-01 钦定）。理由是 issue 锚的是 main 上 spec 的**永久链接**，必须等 spec 定稿合并；反过来先开 issue 后合并 PR，issue 引用的 spec 还在被审核修订，两边互相引着改，麻烦。代码实施是 issue 开出来后认领——认领动作与防撞锁见第 7 步「认领与防撞车」
- 正文**只放 spec 链接 + 改动面 + 验收要点（checkbox 先不勾），不贴 spec 全文**
- spec 链接用合并后 main 上的永久链接
- spec 头部的 `Issue:` 字段回填 issue 号（此时已知号，一并提交或小 PR 补上）

## 闸（两段共用）

**闸按交付物过：每份交付物各过一道，档位当场点；不存在可沿用的档位。**
**摆完卡这道闸即为过完**——用户点「不审」也算过完。段一第 4 步、段二第 9 步都是这道闸，规则同一套：

- **不许自动派，也不许自己判断该不该审**。派审核 agent 是用户的动作，不是 agent 的动作——
  **连「这次算不算核心」也不由 agent 判，判断权整个在用户手里。**
- **每次走到这一步，都用选择卡把档位摆给用户点**（别打字问「要不要审」这种是非题）：

  | 档 | **每一份**派什么 | **每一份**的大致代价 |
  |---|---|---|
  | 不审 | 无，agent 自己过一遍 | 0 |
  | 轻审 | 1 个（**spec 期 = 规格符合性核对 + 两张清单完整性；实施期 = 对抗式找 bug + 范围外对账**） | 约 5 分钟 |
  | 重审 | 2 个（规格符合性 + 对抗式找 bug） | 约 10 分钟 |
  | 重审 + 变异 | 2 个 agent，外加 agent 自己跑一次变异 | 约 10 分钟 + 18 秒 |

  ⚠️ **清单完整性 / 范围外对账两项不新增派发**——轻审由那 1 个 agent 一并做；重审并进「规格符合性」那一路，不新增第三路。
  ⚠️ **档位是「每一份」的强度，多份按份数叠加**（一份 spec 一个 agent、多份并行派）。
  摆卡时**必须把账算给用户看**：「本次 **N** 份 × 该档 = 约 **X** 个 agent / **Y** 分钟」——
  **不是只报单份的数**（5 份子 spec 点轻审 = 5 个 agent，不是 1 个）。
  这是代价不对称：按总数算会让每份被**静默审浅**（用户以为审过了），按每份算最多是**贵，而且当场看得见**。
  ⚠️ **轻审只有 1 个 agent，定位按阶段劈**：spec 期审的是文档，严重发现压倒性是事实/符合类；实施期审的是代码，压倒性是对抗类
  （2026-09-21 复盘一周 4 仓约 30 次实战审核粗计：spec 期约 25+ 比 6-8，实施期约 15+ 比 3；#820 两路对照严重发现零交集）。
  重审两路俱全不用劈；摆卡时按当前阶段直接写对应定位。
  实施期那侧扩了一项（**定位以上表为准**）——两项同属那 1 个 agent 的活，**派发数不变**。
  ⚠️ **「变异」不是 subagent**：把判据换成错的写法、跑一遍全量测试看红不红（全绿 = 这条判据没有护栏）。
  它**只跟「重审」捆绑出售**——卡上摆出来就是「重审 + 变异」这一整档，没有独立开关。
- ⛔ **项目规矩明令禁止某一档时，那一档不摆**——**以项目为准，别拿本流程去压它**。
  被禁的那一档不摆，派审核的 prompt 里也不许要求 agent 去跑它。
  哪个仓禁了哪一档，**问那个仓自己的指令**——本 skill 不登记、不攒名单，也不去猜。
- **用户点了哪档就按哪档做**；没点 / 点了别的 → **按「不审」走，直接往合并走**：不阻塞、不追问、不换个说法再问。
- **交付物口径**——**spec、实施成果（代码或交付型文档）必过闸**；**回填 issue 号 / 状态行 / 索引等无新内容的机械文档变更免过闸**（依据是本条列举式豁免本身，与「文档 PR 合并长期授权」是两件事、互不引用当依据）。
  **交付物 = 随 PR 进入目标仓的改动**；不进仓的文件（/tmp 草稿、PR 正文底稿、会话中间产物）不是交付物，不过闸、不摆卡。
  **无 PR 可走的实施成果**（非 git 仓、目标仓无远端等）**不在上句排除内**——仍是交付物、照摆闸卡，「合并」读作「交付」（交付前摆卡，diff 随卡给用户过目）；算不算「临时产物」判不明的，照旧按下一条「归边不明」处理（闸卡附带一问）。
  归边不明的**不由 agent 自判**——摆闸卡时把归边问题作为卡上附带一问摆给用户（灰区代价是一次点击，漏的代价是静默合并）。
  ⚠️ **「例外」节那条「改一行 / 直接干」免的是方案期那一段**（不出 spec、不开 issue 那一套）；**交付物该过闸照过**——别拿「我这是改一行」当跳过摆卡的理由。
- **文档检查入闸**：交付物含文档改动（口径见下）时，**摆闸卡前先在交付物自己的工作区跑目标仓的文档检查入口，把结果写进闸卡**——固定一行「文档检查：绿 / 红+摘要 / 无入口」；无论用户点哪档（含「不审」）都先跑再摆卡。
  - **触发口径**：交付物本身是 spec / 交付型文档（纯文档 PR）→ 必跑；代码 PR 的 diff 含文档文件改动 → 也跑（宽口径）；免过闸的机械文档变更 → 免跑（免过闸者优先——免过闸者本无闸卡，检查挂在「摆闸卡前」，自然不沾）。归边不明照旧——闸卡附带一问摆给用户，**检查先照跑**。本口径复用上方「交付物口径」条的既有归边，只加「代码 PR 看 diff」一条，不另立第三套归边规则。
  - **入口发现（约定优于配置）**：① 目标仓指令文件（AGENTS.md / CLAUDE.md）里自报的文档检查入口（**须指向真实存在的路径；自报路径不存在 = 该档失效，落到下一档**）→ ② 约定路径按序取第一个存在的：`scripts/check-docs.sh`、`scripts/check-doc-links.sh` → ③ 都没有：闸卡该行写「该仓无文档检查入口」，**不算红**。
  - **红的行为**：红按归因分两类——**本次交付物 diff 引入的红** → 修完重跑转绿后**重摆闸卡**再合并；**仓内既有存量红**（与本次改动面无关）→ 摘要行标注「存量红 N 处，与本次改动面无关」，**不阻本次合并**，同卡报给用户处置。脚本存在但无法执行 / 跑崩溃也算红（原因写进摘要行——存在但跑不起来 = 检查能力坏了，标「无入口」是把坏装成没有）；用户可在闸卡上当场明示豁免一次，豁免理由写进卡。
- **一卡盖批须声明**：一次出 N 份实施 PR，允许一张卡盖本批，但卡上必须写明「共 **N** 份 × 该档 = 约 **X** 个 agent / **Y** 分钟」（**X** 按档位表逐份算：不审 0 / 轻审 1 / 重审 2），并**列明批次成员**（PR / 分支清单）；**未声明份数的，每份各过一道**；**批卡时每个触发文档检查的成员各在自己的工作区跑一次，结果行逐成员列出**（如「A 绿 / B 红+摘要」）。
  （只限实施期同批 PR；spec 期多份子 spec 的「按份数叠加」既有规矩不动。）
- **长期授权与闸分家**：「只含 spec / 文档的 PR 合并长期授权」那两条管的是**「合并动作要不要再问」**，**不管闸**——闸管「审不审、哪一档」。两件事互不引用当依据。
- **执行时按老规矩**（任一档）：
  - **只读审、不许改文件**——审核产出是发现清单，不是改好的文件
  - 要求逐条核对 spec 里的每个 `file:line` 引用和「现状是 X」断言——实测这样能抓出作者自己没发现的事实错误（2026-07-30 两份 spec 共 25 条发现，其中 4 条是 spec 作者写错的事实）
  - 给 subagent 足够上下文：spec 路径、代码在哪个 worktree、已实测过不必复核的数据
  - 修订时在 spec 末尾留「审核修订记录」表格（# / 严重度 / 发现 / 处置）
  - **范围外对账只报「spec 改动面之外的变更」**，不扩展成质量意见——审核 prompt 写狠了会反向逼实施方加防御层（[Anthropic 官方明示的代价](https://code.claude.com/docs/en/best-practices)）
  - **「不审」档不是无账**：实施 agent 合并 PR 前自己拿 diff 对 spec 改动面过一遍；发现范围外变更必须停下报给用户处置，不许静默留下。零成本、只报异常、不算派审核（不破「档位用户点」）
- **没做审核 ≠ 流程没走完**：后续步骤照走，不用在 spec 或 PR 里写「未经审核」之类的免责声明。

## 各步细则·段二（实施期）

**第 7 步（认领与防撞车）——锁由 git 保管，不靠登记、不靠吱声：**

手里有 issue 号就从这段进，段一与你无关——spec 已在 main。

- **命名 = 锁**：分支和 worktree 目录同名 `issue-<N>`，纯号、无 slug、无前缀。同一 `.git` 里分支名唯一、已被检出的分支不许再检出——git 强制（2026-09-16 实测：5 进程并发抢同号，恰好 1 个开成，其余全被 fatal 拒）。**认领就是开 worktree**：开得成 = 没人做；被拒 = 有人在做。
- **认领**：`git fetch origin --prune` 后 `git worktree add <wt根>/issue-<N> -b issue-<N> origin/main`（wt 根项目自定，如 `.claude/worktrees/`；从 origin/main 建，本地 main 可能落后）。想早知道可先 `git worktree list | grep issue-<N>`——可选，锁不靠它。⚠️ 别拿 issue `open` 当「没人认领」：open 同时表示待做和在做（xhs-analysis #12 实测：两个 agent 都据此判断「没人做」，双双做完浪费一份）——只信锁。
- **撞锁 = 停**：`fatal: a branch named 'issue-<N>' already exists` = 有人领过。`git worktree list` 找到对方工作区路径：目录还在 → 人在做，去读该 issue 的评论定等 / 让；目录没了 → 对方已不在，`git worktree prune` 清掉残留登记后接手：`git worktree add <wt根>/issue-<N> issue-<N>`（不带 -b 复用旧分支 = 接着对方的提交干；想推倒重来就先 `git branch -D issue-<N>` 再带 -b 新建）。
- **放锁**：PR 合并后 `git worktree remove <wt根>/issue-<N>` + `git branch -d issue-<N>`（`-d` 认的是 fetch 后的 origin/main，先 `git fetch origin`；远端 squash 合并时 `-d` 照样以 not fully merged 拒删——换 `-D`，2026-09-16 实测）。不放 = issue 重开时永远撞锁。
- **进 worktree 第一件事重新取基准**（`pwd`）：编辑工具用的绝对路径不跟 cwd 走，照会话前半段记下的主仓路径写，改的是主仓、还没报错。快信号：新写的测试报 `no tests to run`、新符号 grep 不到、worktree 里 `git status` 是空的。
- **边界**：锁只挡同号认领——不同 issue 改同一堆文件拦不住（那是拆 spec 时「独占文件」规矩管的，见 `references/split-large-spec.md`）；跨机 / 跨 clone 不共享 `.git`，无锁；`git worktree add --force` 能强拆——本机制只防守规矩的人犯错，不防故意绕过。没有 issue 号的活：wt 照开（见下面「工作区规矩」）但无锁可领，命名随意（惯例 `<type>/<slug>`）。

**第 8 步（实施）：**

在 worktree 里干活，规矩见下面「工作区规矩」节。

**第 9 步（过闸 → 合并 → 回填）：**

- **过闸**：实施 PR 合并前摆卡（见闸节）——两段同一道闸、同一套档位，档位当场点
- **合并**：按「Git: 危险操作」的规矩走——代码 PR 不在「只含 spec / 文档的 PR」长期授权内
- **回填**：合并后回头把 issue 里已满足的验收项勾掉 + 评论一句「代码已合」，并删 worktree 放锁（见第 7 步）

## 工作区规矩：凡文件变更，先开 worktree

**默认**：凡涉及文件变更——spec、代码、文档都一样——**先 `git fetch`，从 `origin/main`（或 `origin/master`）开 worktree** 再动手，别在主仓改。**本地 main 可能落后，别拿它当基准。** 主仓的工作区、暂存区、HEAD 是全仓共享的，多 agent 并行时互踩没有兜底。

**豁免只有一种——当场豁免**：用户说「直接改 / 不开 wt」→ 本次在主仓改。只由用户发起，agent 不自己判、不主动建议——宁可啰嗦，不可漏锁。

**两条边界：**

- **真并行多 agent 的仓（指令文件 `AGENTS.md` / `CLAUDE.md` 声明了的），豁免不免锁**：issue 认领一律走第 7 步「认领与防撞车」——锁就是 wt 本身，免 wt = 拆锁。
- **豁免只免「开 wt」一个动作**，不免两段流程的其他环节（该出 spec 照出；「直接干」免的是**方案期那一段**，见下面「例外」节——那是另一条例，不混）。

## 分工边界

本 skill 只管流程骨架。**项目专属的体例**（spec 放哪个目录、issue 标题格式、用哪些标签、正文节序）由该项目的指令文件（`AGENTS.md` / `CLAUDE.md`）或 agent 记忆承载——先查这两处有没有 house style（如 xhs-analysis 的 `issue-and-spec-house-style` 记忆条目），有就照它写。

⚠️ **本 skill 里出现的具体仓名，只准是「某条规矩的一次性证据」，不准攒成可追加的登记表**——仓自己改了规矩，没人会回来改这里，登记表只会越挂越旧。2026-09-16 那张逐仓豁免名单就是因此整张删掉的（来龙去脉见 `references/adoption-model.md`）。

## 应用到项目

把目标项目**一句话切成就地指向 spec-flow 的采纳形态**：项目里只放指针、不放流程副本。

**触发**：用户说「把 spec-flow 应用到（本）项目」（可带附加描述，如「同时启用 ADR」），或显式 `/skill:spec-flow 应用到项目 …`。

**这是用户可触发功能，不是两段流程的一步**——它不改流程的步数。没被要求就别自作主张应用到当前仓。

**执行五步**：

1. **检测现状**——指令文件有无、旧副本 / 旧流程段有无。
2. **判目标文件**——见下。
3. **写指针块**——模板取 `references/apply-to-project.md`。
4. **解析附加描述**——见下。
5. **报告结果**——含检测到的旧物清单与处置建议。

**目标文件判定**：指针本体只写 `AGENTS.md`（没有就新建）。

- 项目已有 `CLAUDE.md` → 只补一行 `@AGENTS.md` 导入（是单独一行、**不包反引号或代码块**——包了会静默失效，不报错不提示）；已存在则不重复追加。
- 没有 `CLAUDE.md` → **不新建**。Claude Code 2.1.277+ 在项目无 `CLAUDE.md` 时原生读 `AGENTS.md`，kimi 也原生读它——新建一份反而多一个要维护的落点。

**附加描述**：已注册修饰词 `adr`（行为见 `references/apply-to-project.md`）；**不认识的描述当场问用户，不猜**。

**旧副本 / 旧流程段**：检测到**逐字 / 整段摘抄副本**（通常是 `docs/spec-flow.md`）、或指令文件里已有**复述流程正文**的段落 → **报告给用户定夺，不自动删改**。判定口径（特征句、以及「只提名字的指针句不算」）见 `references/apply-to-project.md`。

模板全文（判定树与幂等规则、指针块、`CLAUDE.md` 导入行、ADR 入口节、规则页骨架、附加描述规则、旧副本检测口径）在 `references/apply-to-project.md`——**照它取，别现编**。

## 参考（非流程）

spec 一份装不下、要拆成几份子 spec 时，以前的做法记在 `references/split-large-spec.md`。

**那是备忘录，不是流程** —— 两段流程不因它变形（照样 spec → 合并 → issue，只是可能多出几份子 spec）。
要不要拆、怎么拆，**以当时说定的为准**；没读它，两段流程照走。

ADR（跨任务长期决策记录）的可选做法记在 `references/adr-decision-records.md`。

**它也是备忘录，默认不启用**：agent 不得因为项目看起来复杂就自行引入 ADR，也不主动追问「要不要启用 ADR」。
只有用户明确要求「给这个项目启用 / 接入 ADR」、明确要求本次写 ADR，或项目现有规则已声明采用时，才读取并按它执行。
项目级采用必须把持久入口落在项目自己的指令与文档中；不在本 skill 里维护采用项目名单。

「spec-flow 怎么被一个项目采用」（**只指向式** / 同步 + 指向 / 不引用 / 当场引用）的设计与历次演进记在 `references/adoption-model.md`——同样是备忘录，不是流程；采纳动作发生在项目侧，不发生在这份文件里。**新仓默认走只指向式**，由上面〈应用到项目〉产出。

文档、spec、ADR、issue / PR 的分工、spec 基线后的变更分流，以及让现状文档少而新的几条约束，记在 `references/documentation-lifecycle.md`。**这是可选备忘录，默认不启用**；仅用户明确要求，或项目规则已声明采用时，才读取并执行。项目只留指针和本地 owner 映射，不复制本备忘录正文。

## 例外

改一行、纯查一个事实、或用户明确说「直接干 / 别走流程」时，**只免方案期那一段**（不出 spec、不开 issue）——**不免闸**：**交付物照样过闸**（见闸节）。「不开 wt」的**工作区**豁免是另一条，见上面「工作区规矩」——两条例外各管各的，不混。
