# spec：交付物口径明文化——仓外临时产物不是交付物、不过闸

- 日期：2026-09-24
- Issue：#29
- 状态：待实施

## 背景：要解什么问题

2026-09-24 #25 实施现场：实施 agent 开出 PR #27 后照闸节摆档位卡，owner 疑惑「这种文档其实没必要审」——把 `/tmp` 里的 PR 正文底稿（仓外临时产物）和 PR 本身（仓内交付物）混成了一份，进而怀疑卡弹错、提议「只审核项目内的文档」。当场对账结论：卡弹得对（PR #27 是实施成果、必过闸），仓外草稿本就无卡。

但现行「交付物口径」条（SKILL.md:107-109）只从正面列举——spec、实施成果必过闸，机械文档变更免过闸——**没有一句从边界上明排「不进仓的文件不算交付物」**。判定靠 agent 现场发挥，就会漂成两个方向：给底稿也摆张卡（多），或以为文档 PR 不该有卡（漏）。本单管的是前者这一类混淆的根治。

owner 拍板（2026-09-24 对话，原话「单开一个 spec 和 issue 来处理这件事」）：把边界句明文化。（主 agent 当场建议过「不改」，owner 裁决仍要明文化——记录在案。）

## 机制清单

**无新增机制**——本 spec 只对既有「交付物口径」作明文化：把隐含的边界判定写成明文，不引入新概念、新抽象、新归边规则。

## 改动面（一处）

SKILL.md 闸节「交付物口径」条，主句（现 107 行）之后、「归边不明」行（现 108 行）之前，新增一行：

```
  **交付物 = 随 PR 进入目标仓的改动**；不进仓的文件（/tmp 草稿、PR 正文底稿、会话中间产物）不是交付物，不过闸、不摆卡。
```

既有条文其余一字不动。

**回响自查（第 5 步硬规矩）**：纯加一句边界定义，不改既有判据 / 定义 / 术语。已 grep「交付物」全仓（2026-09-24，#27 合并后的 main，按出现次数口径）：SKILL.md 12 处（本改动新加一行含 2 处，改后 14；其余无需动）、`README.md` 与 `references/` 四文件零命中、兄弟 spec 四份中两份命中共 32 处（另两份零命中），命中的均为已实施历史时点记录、不同步。无需第二处改动。

## 不做清单

- 不改「必过闸 / 免过闸」两套既有归边，只补边界句。
- 不因本句改变任何在途 PR 的闸——PR #27 照常过闸合并（本 spec 起草时它已合并）。
- 不立「仓内但不进 PR 的文件」的任何规矩（untracked 草稿、临时脚本等）——它们同样不随 PR 进仓，边界句已覆盖，不另举例膨胀条文。
- 不改 frontmatter description、不改「文档检查入闸」条（其触发口径复用本条的既有归边，边界句对它同向生效、无需回指）。

## 验收标准

- [ ] 闸节「交付物口径」条主句后含「交付物 = 随 PR 进入目标仓的改动」边界句与三个仓外例子（/tmp 草稿、PR 正文底稿、会话中间产物）。
- [ ] diff 仅此一处新增行，既有条文零改动。
- [ ] 回响复核：grep「交付物」（按出现次数口径）落点与 spec 所述一致，无第二处需同步。

## 自定细节（本 spec 里替用户定掉的点，不同意就 veto）

| # | 自定细节 | 理由 |
|---|---|---|
| D1 | spec 落 `docs/specs/2026-09-24-deliverable-scope.md` | 仓体例（`docs/specs/<日期>-<slug>.md`） |
| D2 | worktree 根 `~/code/spec-flow.worktrees/`、分支 `docs/deliverable-scope` | 无 issue 号的活命名惯例 `<type>/<slug>`；沿用既有 D2 |
| D3 | Issue 字段先空，spec PR 合并开 issue 后回填 | 第 6 步时机规矩（owner 2026-09-01 钦定） |
| D4 | 仓外例子列三个：/tmp 草稿、PR 正文底稿、会话中间产物——作封闭三例，不加「等」字 | 取自本次现场最易混的三类；覆盖兜底靠括号前的一般性条款「不进仓的文件…不是交付物」，举例开闭不影响范围 |
| D5 | 落点在「交付物口径」条内新增一行，而非闸节新开一条 | 它是同一条口径的边界，新开一条 = 制造第二个引用点，改一个漏一个 |
| D6 | 边界句用「随 PR 进入目标仓」而非「项目内的文件」 | owner 提议的「项目内」会被误读成物理位置（仓内 untracked 草稿也算项目内）；判准是「进不进 PR」，不是「在不在仓」 |

## 审核修订记录

2026-09-24 spec 期**轻审**（1 个独立 agent：规格符合性核对 + 两张清单完整性），3 条发现（0 严重 / 1 中 / 2 轻），当日全数修订。

| # | 严重度 | 发现 | 处置 |
|---|---|---|---|
| 1 | 中 | 回响自查计数口径自相矛盾：「SKILL.md 12 处」只在按出现次数口径成立（按行 = 9）；「本改动只加其一」两种口径都不对——新行含「交付物」×2，应为 12→14；验收第 3 条照此复核会出假不符 | 回响自查改按出现次数口径写明「12 处 → 改后 14」；验收第 3 条同步口径 |
| 2 | 轻 | 「兄弟 spec 两份共 32 处」字面歧义——兄弟 spec 实为四份，另两份零命中未点明，易读成「统共两份」 | 改「四份中两份命中共 32 处（另两份零命中）」 |
| 3 | 轻 | D4 理由文字方向相反：「举例不是枚举」与「不加以等字防开口列举」自相矛盾 | D4 改「封闭三例；覆盖兜底靠括号前一般性条款」 |
