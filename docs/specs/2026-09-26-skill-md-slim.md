# spec：主文件瘦身——案例进依据档案、重复条款合并、关键规则前置、加体量护栏

- 日期：2026-09-26
- Issue：#37
- 状态：待合并（spec 期轻审 16 条 + 1 条保留意见，全数处置，见文末「审核修订记录」）

## 背景：要解什么问题

### 现状（2026-09-26 实测，字数口径见 D4）

- `SKILL.md` 全文 214 行，frontmatter 占第 1–4 行；**正文 = 第 5–214 行，共 210 行、10,497 字**（`tail -n +5 SKILL.md | wc -m`）。#37 正文写的 10,498 多计了 frontmatter 结束行的换行，差 1 字。
- 最大一节「闸（两段共用）」3,545 字；次大段一 1,896、段二 1,732（节 = 标题行到下一个标题前，含空行）。
- 正文警示符 13 处（⚠️ 11、⛔ 2），另有 ⏰ 1 处；加粗 162 对。
- 增长（逐提交实测，各取当日末次提交）：09-10 1,386 → 09-16 4,712 → 09-21 6,064 → 09-24 10,233 → 09-26 10,497。09-21 至 09-26 这 6 天净增 5,785 字，日均约 960。

### 加载机制（官方文档原文，2026-09-26 已打开核对）

- Claude Code：技能被调用时正文整份进上下文，**之后不再重读**；自动压缩后「re-attaches the most recent invocation of each skill after the summary, keeping the first 5,000 tokens of each」（[skills 文档](https://code.claude.com/docs/en/skills)）。
- kimi：现役 kimi-code 源码把 SKILL.md 正文整份包进 `<skill-loaded>` 块注入；references 靠模型按需读（[kimi-code skills 文档](https://github.com/MoonshotAI/kimi-code/blob/main/docs/en/customization/skills.md)）。
- 含义：214 行离官方「500 行」线还远，**真正的约束是 token**。1.05 万中文字估算在 5k token 以上（**未实测**，本机无计数途径），超出部分在长会话压缩后被截掉，越靠后越先丢。按现在的顺序，「段二·实施期」起点在正文第 5,988 字（约 57% 处），其后还有工作区规矩、分工边界、应用到项目、参考四节，排在最末的是载着「不免闸」的「例外」（起点第 10,355 字）。

### 互联网调研（信源分档：官方一手 > 论文 > 其他）

| 说法 | 出处 | 对本 spec 的含义 |
|---|---|---|
| 「Bloated CLAUDE.md files cause Claude to ignore your actual instructions!」；「If you emphasize many lines, none of them stands out.」 | [Claude Code best-practices](https://code.claude.com/docs/en/best-practices) | 体量和强调密度都要压 |
| 「Keep SKILL.md body under 500 lines」；「The context window is a public good.」；「Only add context Claude doesn't already have.」；「Keep references one level deep」 | [Agent Skills best practices](https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices) | 细节进 references、只挂一层 |
| 「Providing context or motivation behind your instructions … can help Claude better understand your goals」；「Claude is smart enough to generalize from the explanation.」 | [Prompting best practices](https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices) | **理由不能挪走**：规则旁留一句「为什么」 |
| 「The fix is to dial back any aggressive language.」；skill-creator：「If you find yourself writing ALWAYS or NEVER in all caps … that's a yellow flag」 | 同上；[anthropics/skills skill-creator](https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md) | 警示符收敛到少数几条 |
| IFScale 摘要：「bias towards earlier instructions」；Lost in the Middle 摘要：「performance is often highest when relevant information occurs at the beginning or end of the input context」 | [IFScale, arXiv 2507.11538](https://arxiv.org/abs/2507.11538)；[Lost in the Middle, arXiv 2307.03172](https://arxiv.org/abs/2307.03172) | 关键规则前置。坑：IFScale 测的是「keyword-inclusion instructions」，不是行为规则 |

### 关键发现：只挪案例不够

逐段盘点（2026-09-26，按 R1–R9 挪出的原文逐段实测）：案例、统计、日期、来龙去脉合计约 720–740 字（随切分口径浮动），只占正文 7%；扣掉留下的一句理由和指针，净减更少。体量大头在**重复条款**（同一规则在 3–4 处重述）和**两个非流程节**（〈应用到项目〉983 字、〈参考〉817 字，前者与 `references/apply-to-project.md` 重叠）。

### owner 拍板（2026-09-26 对话，卡片记录）

瘦身力度选 **B 档**（挪案例 + 去重 + 两个非流程节缩成指针 + 关键规则前置，目标约 7.5k 字）；机制清单 1–4 **全要**。C 档（条件细则外挪）不做，等实测 token 后另议。

## 机制清单（本 spec 新增的机制）

| # | 机制 | 解决什么 / 没有它会怎样 |
|---|---|---|
| 1 | **依据档案**：新建 `references/rationale.md`，收挪出的案例、统计、来龙去脉，按 R 编号；主文件规则旁只留一句理由 +「（依据：rationale Rn）」 | 案例有处可放、来历不丢 / 没有它：要么删掉丢来历，要么留在主文件继续胖 |
| 2 | **体量护栏**：本仓新建 `scripts/check-docs.sh`，正文超预算或警示符超限就红；闸节「文档检查入闸」的约定路径 ② 会在每次摆闸卡前自动跑它（免过闸的机械文档变更不摆卡，也就不跑） | 瘦完不回涨 / 没有它：照 09-21 以来日均约 960 字的速度，几天就涨回 1 万字 |
| 3 | **关键规则前置**：章节重排，「例外」「闸」挪到总览之后 | 压缩只留前 5k token 时核心规则仍在 / 没有它：长会话后越靠后的规则越先丢，「不免闸」排在最末 |
| 4 | **强调收敛**：警示符 13 → ≤ 3，加粗 162 → ≤ 80 对 | 真正要紧的几条重新显眼 / 没有它：官方原话「处处强调等于没有强调」 |
| 5 | **规则对照表**（审核 #7 补收，owner 待确认）：实施 PR 正文附一张表，旧正文每条 bullet / 段落 → 新位置 / 并入哪组 / 挪到哪个 R | 「规则语义不变」有逐条可核的依据 / 没有它：审核只能凭印象判断瘦身有没有丢规则 |

## 改动面

B 档定义：**不删任何规则、不改任何判据语义**；只挪案例、合并重复、缩非流程节、重排、收敛强调。

本 spec 里 `:N` 均指改前 `SKILL.md`（main `dd109e9`）的全文行号，含 frontmatter。

### 1. 章节重排（机制 3）

新顺序：总览 → **例外** → **闸（两段共用）** → 工作区规矩 → 各步细则·段一 → 各步细则·段二 → 分工边界 → 应用到项目 → 参考（非流程）。

- 章节标题一字不改（`references/split-large-spec.md:71,73`、`references/adoption-model.md:32` 按名引用，锚点不废）。
- 跨节方位词共五处，全部改成按名引、去掉「上面 / 下面」：`:10`「见下面闸节」、`:139`「见下面『工作区规矩』」、`:143`「见下面『工作区规矩』节」、`:160`「见下面『例外』节」（该处随 G3 整句删）、`:214`「见上面『工作区规矩』」。节内的「上方 / 下一条」不动。

### 2. 案例 / 统计 / 来龙去脉 → 依据档案（机制 1）

挪出内容**逐字**搬进 `references/rationale.md`，不改写；主文件留下的那一句理由见 D8。

| R | 原位置 | 挪走的 / 主文件留下的一句理由 |
|---|---|---|
| R1 | `:61-63` 第 5 步「为什么有这条」 | 挪：2026-09-11 反爬 CIDR→ASN 实例 / 留：只改判据那一句、别处不同步，照 spec 施工等于白改 |
| R2 | `:71` 第 6 步时机 | 挪：「owner 2026-09-01 钦定」+ 原理由全文 / 留：issue 锚 main 上 spec 的永久链接，spec 没定稿就开 issue 会两边互相引着改 |
| R3 | `:97-98` 轻审定位 | 挪：2026-09-21 复盘数字（25+ 比 6-8 等、#820）/ 留：spec 期严重发现以事实 / 符合类为主，实施期以对抗类为主 |
| R4 | `:121` 逐条核对 file:line | 挪：2026-07-30 两份 spec 25 条发现、4 条事实错误 / 留：能抓出作者自己没发现的事实错误 |
| R5 | `:124` 范围外对账 | 挪：Anthropic 官方链接 / 留：审核 prompt 写狠了会反向逼实施方加防御层 |
| R6 | `:134` 命名 = 锁 | 挪：2026-09-16 五进程并发实测 / 留：git 强制 |
| R7 | `:135` 别拿 open 当没人认领 | 挪：xhs-analysis #12 双双做完 / 留：open 同时表示待做和在做 |
| R8 | `:137` 放锁 | 挪：2026-09-16 squash 实测 / 留：squash 合并时 `-d` 拒删，换 `-D`（操作指令本身不动） |
| R9 | `:164,166` 分工边界 | 挪：xhs-analysis house style 例子、2026-09-16 豁免名单删除始末 / 留：仓自己改了规矩没人回来改这里，登记表只会越挂越旧 |
| R10 | （新增） | 本 spec 的调研表与预算依据——体量护栏报红时指向这里 |

档案头部写明：备忘录不是规程，`SKILL.md` 不依赖它；新增规则时案例写这里、主文件只留一句理由。

### 3. 去重：每组只留一处完整陈述

| 组 | 现在出现在 | 处理 |
|---|---|---|
| G1 长期授权与闸分家 | `:67`、`:107` 括注、`:118` | 留 `:118`；`:67` 缩成「长期授权，不用每次问（不管闸，见闸节）」；`:107` 括注缩成「（依据是本条列举式豁免本身）」，删后半句「与…两件事、互不引用当依据」（由 `:118` 覆盖） |
| G2 代码 PR 不在授权内 | `:68`、`:148` | 留 `:148`（代码 PR 在段二合并）；删 `:68` |
| G3 例外只免方案期、不免闸 | `:111`、`:160`、`:214` | 留「例外」节（已前置到闸之前）；`:111` 删；`:160` 只留「豁免只免开 wt 一个动作，不免两段流程的其他环节」 |
| G4 两项不新增派发 | `:92`、`:100` 后半 | 留 `:92`；`:100` 只删后半「——两项同属那 1 个 agent 的活，派发数不变」，前半「实施期那侧扩了一项（定位以上表为准）」是定位条的回指，保留 |
| G5 两段同一道闸、档位当场点 | 总览 `:27`、第 4 步 `:39`、`:78-79`、`:147` | 留总览一句 + 闸节开头；第 4 步缩成「spec PR 合并前过闸（见闸节），审的是 spec 本身」；`:147` 缩成「实施 PR 合并前过闸（见闸节）」 |

### 4. 两个非流程节缩成指针

- **〈应用到项目〉**（983 → 约 350 字）保留六项：① 功能一句；② 触发；③「不是流程步骤，没被要求别自作主张」；④ 五步名称 +「判定树、模板、幂等规则全在 `references/apply-to-project.md`，照它取，别现编」；⑤ `:189` 附加描述整行（含「不认识的描述当场问用户，不猜」，`references/apply-to-project.md:168` 引它）；⑥「检测到旧副本 / 复述流程的段落 → 报告用户定夺，不自动删改」。「目标文件判定」三条细则只留 references 一份（见 D10）。
- **〈参考（非流程）〉**（817 → 约 400 字）改成两列表「备忘录 | 何时读 / 约束」，共六份（含新增 rationale）。下列约束**原样保留在表内**：
  - ADR：默认不启用；不得因项目复杂自行引入、不主动追问；仅三种情况启用（用户明确要求启用、明确要求本次写 ADR、项目规则已声明采用）；不在本 skill 维护采用项目名单。
  - 文档生命周期：默认不启用；仅用户明确要求或项目规则已声明采用时读；项目采用只留指针、一行默认读序和本地 owner 映射，不复制正文。
  - 拆大 spec：备忘录不是流程；两段流程不因它变形；要不要拆以当时说定的为准。
  - 采纳模型：新仓默认走只指向式，由〈应用到项目〉产出。

### 5. 强调收敛（机制 4）

- 警示符只留 3 处（见 D5），其余去掉符号、保留句子；⏰ 去掉。
- 加粗：每条 bullet / 段落至多一处，加在判据词上；计数口径见 D16。

### 6. 体量护栏脚本（机制 2）

`scripts/check-docs.sh`（bash，内部用 python3 数 Unicode 字符）：

1. `SKILL.md` 正文字符数（D4 口径）≤ 8,000，否则红，提示「新增的案例 / 统计 / 来龙去脉进 `references/rationale.md`，规则旁只留一句理由；依据见 rationale R10」。
2. 正文 ⚠️ + ⛔ 个数 ≤ 3，否则红。
3. `SKILL.md` 里出现的 `references/*.md` 路径都存在，否则红。

每项一行结果，末行「绿 / 红」，退出码 0 / 1。预算常量只写在脚本顶部一处。

### 7. 回响同步

- `references/apply-to-project.md:3`「`SKILL.md` 只留判定规则与指向」→「只留触发、五步骨架与指向」。
- `README.md` 目录加 `references/rationale.md`、`scripts/check-docs.sh` 两行。

## 回响自查（第 5 步硬规矩）

本 spec 不改任何既有判据的语义，但**新增了常量**（8,000 / 3 / 1,000 / 1,500）和**指针语法**，所以六个落点逐一表态：

| 落点 | 表态 |
|---|---|
| 机制 / 原理节 | 新常量只落在本 spec（D4 / D5 / D14）和护栏脚本顶部；`SKILL.md` 正文不写预算数字（D12）。grep「8,000」「预算」全仓（本 spec 除外）零命中；「体量」仅 `split-large-spec.md:79` 一处，指拆 spec 的客观信号，与本预算无关；`split-large-spec.md:23` 的「>500 行」同理是另一件事 |
| 实现 / 数据源节 | 字数口径统一为 D4，护栏脚本与本 spec 同口径；`scripts/check-docs.sh` 的既有落点 = 闸节 `:114` 约定路径 ②（本仓根无 `AGENTS.md` / `CLAUDE.md`，路径 ① 不触发）+ README 目录（改动面 7） |
| 实施步骤 | 新建两件：`scripts/check-docs.sh`、`references/rationale.md`；改三件：`SKILL.md`、`references/apply-to-project.md:3`、`README.md` 目录 |
| 验收标准 | 每条改动面都有对应验收项：改动面 1 → 验收 4、5；2 → 6；3 → 7；4 → 8、9；5 → 10、11；6 → 1、2；7 → 13；机制 5 → 12 |
| 跨文档引用 | `references/split-large-spec.md:71`（闸节）、`:73`（第 7 步「认领与防撞车」）、`references/adoption-model.md:32`（〈应用到项目〉功能）→ 标题不改，不受影响；`references/apply-to-project.md:3` → 需改（改动面 7）；`:168`（附加描述一行）→ 该行整行保留，不受影响 |
| 历史记录节 | 历史 spec 五份中三份按行号引用 `SKILL.md`（两段一闸 7 处、交付物口径 3 处、文档检查入闸 1 处），另两份零命中。它们是已实施的历史时点记录，按惯例不同步；本 spec 不推翻任何旧裁决、只搬位置，不需要标「已被取代」 |

另：frontmatter description 含「⛔ agent 不得自派审核」，不在正文口径内，不动。落地物 `scripts/check-docs.sh`、`references/rationale.md` 现在都不存在，实施时新建，验收逐项核。

## 不做清单

- 不删规则、不改判据语义（B 档定义）；不做 C 档（文档检查入闸、撞锁恢复、审核执行细则外挪），等实测 token 后另议。
- 不改 frontmatter description——它每个会话都常驻，是另一笔账，另议。
- 不改 `references/` 既有备忘录正文（`apply-to-project.md:3` 一句指代除外）；不同步历史 spec。
- 不建 token 计数工具，护栏按字符数。
- 不给别的采纳仓加任何东西；护栏只管本仓。

## 验收标准

- [ ] 1. `scripts/check-docs.sh` 存在、可执行、在实施后的主干上跑出**绿**。
- [ ] 2. 变异三次各红一次：正文补到 8,001 字、多加一个 ⚠️、引一个不存在的 `references/x.md`。
- [ ] 3. 正文 ≤ 8,000 字（D4 口径，目标约 7.5k），报前后字数；有 token 计数途径就报前后 token，没有就在 PR 写明未测。
- [ ] 4. 章节顺序与改动面 1 一致；九个标题与改前逐字一致；「例外」「闸」起点满足 D14。
- [ ] 5. 跨节「见上面 / 见下面」零命中（改动面 1 所列五处全改）。
- [ ] 6. R1–R10 在档案里都有，R1–R9 为原文逐字搬入；主文件对应位置各有一句理由 + 依据指针。
- [ ] 7. G1–G5 每组在主文件只剩一处完整陈述；G1 保留「依据是本条列举式豁免本身」；G4 保留「实施期那侧扩了一项（定位以上表为准）」。
- [ ] 8. 〈应用到项目〉保留改动面 4 所列六项，逐项找得到。
- [ ] 9. 〈参考〉为两列表、六份备忘录齐；改动面 4 所列四组约束逐条在表内。
- [ ] 10. 警示符 = 3 处且为 D5 所列；⏰ 零命中；加粗 ≤ 80 对（D16 口径）。
- [ ] 11. 每条 bullet / 段落加粗 ≤ 1 处（D16 口径，步骤列表和表格除外）。
- [ ] 12. 实施 PR 正文附规则对照表（机制 5，格式见 D15）。
- [ ] 13. `references/apply-to-project.md:3`、`README.md` 目录已同步。

## 自定细节（本 spec 里替用户定掉的点，不同意就 veto）

| # | 自定细节 | 理由 |
|---|---|---|
| D1 | spec 落 `docs/specs/2026-09-26-skill-md-slim.md` | 仓体例 |
| D2 | worktree `~/code/spec-flow.worktrees/docs-slim-skill-spec`、分支 `docs/slim-skill-spec` | spec 期不占实施锁 `issue-37` |
| D3 | Issue 字段直接填 #37，合并后在 #37 回填永久链接、改动面、验收，不另开 issue | #37 已作立项占位先开，正文写明「spec 合并后在这里回填」 |
| D4 | 预算 8,000 字；字数口径 = frontmatter 结束行之后到文件末的全部字符，每行计换行（现状等价 `tail -n +5 SKILL.md \| wc -m`，UTF-8） | 目标约 7.5k，留约 500 余量；口径可用一行命令复现，本 spec 所有字数同此口径 |
| D5 | 警示符保留三处：⛔ 第 5 步回响、⚠️ 档位按份叠加、⚠️ 别拿 open 当没人认领 | 三条都有记录在案的静默失败（反爬白改、静默审浅、xhs #12 重复做） |
| D6 | 加粗 ≤ 80 对只验收一次、不进脚本 | 加粗计数误伤多（表头、步骤名），进脚本会逼人为凑数改排版 |
| D7 | 档案文件名 `rationale.md`，条目 R 编号，主文件指针写「（依据：rationale Rn）」 | 短；编号不随标题改动失效 |
| D8 | 挪出内容逐字搬，不改写；主文件留下的一句理由由实施方提炼措辞，以 R 表「留」列为准绳，语义不许超出 | 搬家时改写 = 同时丢信息又难核；留下那句的措辞交给实施方，但有准绳可核 |
| D9 | 章节标题一字不改 | 外部按名引用的锚点不废 |
| D10 | 〈应用到项目〉的「目标文件判定」三条只留 `references/apply-to-project.md` 一份 | 它们本来就在该文件 §一 / §三，且该功能强制先读模板；这是 B 档里**唯一一处规则条文离开主文件**，属跨文件去重 |
| D11 | 护栏第 3 项查 references 路径存在 | 本次加了一批指向档案的指针，指针断了来历就丢；可 veto |
| D12 | 脚本用 bash + python3，预算常量只写脚本顶部 | macOS / 常见 Linux 自带；数字只一处，不在 README 或 `SKILL.md` 复述 |
| D13 | 去重保留处按改动面 3 表 | 保留在「规则语境最完整」的那一处 |
| D14 | 位置阈值：「例外」起点 ≤ 1,000 字、「闸」起点 ≤ 1,500 字；起点 = 该节标题行首字符在正文中的累计字符偏移（D4 口径） | 按新序预估约 547 / 689；留余量给总览日后微调，又能拦住有人把大节插到前面 |
| D15 | 规则对照表：旧正文每条 bullet、每个独立段落、每张表各算一条；三列「旧位置 \| 新位置 \| 处理（保留 / 并入 Gx / 挪到 Rx / 删-重复）」；放实施 PR 正文，不进仓 | 粒度与审核逐条核对的单位一致；不进仓就不增加主文件或档案体量 |
| D16 | 加粗计数：`**` 出现次数 ÷ 2，全正文计（含表格、步骤列表）；「每条 ≤ 1 处」的检查对象不含总览步骤列表与表格单元格 | 总量口径简单可复现；单条上限只管正文 bullet / 段落，步骤名和表头本来就该醒目 |

## 审核修订记录

### spec 期（2026-09-26）

**轻审**（1 个只读 agent：规格符合性核对 + 两张清单完整性，结论留档于 #37 评论），16 条发现（0 严重 / 8 一般 / 8 提示）+ 1 条保留意见；范围外变更 0。修订 agent 逐条复核，全部成立，当日全数处置。编号沿用审核原编号。

| # | 严重度 | 发现 | 处置 |
|---|---|---|---|
| 1 | 一般 | 跨节方位词漏 `:139`、`:160` 两处，验收「零命中」不可能绿 | 改动面 1 改为五处全列（`:160` 随 G3 整句删）；验收 5 同步 |
| 2 | 一般 | 「五份历史 spec 有按行号引用」不实，实为三份（7 / 3 / 1），两份零命中 | 回响自查「历史记录节」一行改为实测分布 |
| 3 | 一般 | 行数两口径打架（211 / 214），都不是正文行数 | 统一为「全文 214 行，正文第 5–214 行共 210 行」 |
| 4 | 一般 | 闸节字数三方不一致（3,535 / 3,545 / 3,543） | 统一口径（D4 + 节 = 标题行到下一标题前，含空行）重测为 3,545，与 #37 一致；审核的 3,543 是去尾空行口径 |
| 7 | 一般 | 新概念「规则对照表」未进机制清单 | 补为机制 5，标「审核补收，owner 待确认」；格式写进 D15 |
| 8 | 一般 | 位置阈值 1,000 / 1,500 是新常量却未列 D，测量方法未写死 | 新增 D14，写明阈值与「标题行首字符累计偏移」测法 |
| 9 | 一般 | 改动面 4 的承诺无验收托底 | 改动面 4 把保留项逐条列明（六项 + 四组约束）；新增验收 8、9 |
| 10 | 一般 | 回响自查六个落点只覆盖两个，且本 spec 实际新增了常量 | 回响自查改为六落点逐一表态的表格，并承认新增常量与指针语法；审核补充 grep 的「体量全仓零命中」复测有 1 处无关命中（`split-large-spec.md:79`），已如实写入 |
| 5 | 提示 | 分节字数与正文总数有近似差 | 全部按 D4 口径重测：正文 10,497、应用到项目 983、参考 817；注明 #37 的 10,498 差 1 字的来由 |
| 6 | 提示 | 09-21 取的是当日首次提交，与其他日期口径不一 | 增长序列统一取当日末次提交（09-21 = 6,064），日均改为约 960 字 |
| 11 | 提示 | 「每条至多一处加粗」无验收项 | 新增验收 11，口径见 D16 |
| 12 | 提示 | 「章节标题一字不改」无验收项 | 验收 4 加「九个标题与改前逐字一致」 |
| 13 | 提示 | G1 整删 `:107` 括注会丢「依据是本条列举式豁免本身」 | G1 改为括注缩成该半句，只删后半句 |
| 14 | 提示 | G4 整行删 `:100` 会丢「（定位以上表为准）」回指 | G4 改为只删后半句，前半保留；验收 7 点名核对 |
| 15 | 提示 | 「排在最后的恰是段二和例外」不准确 | 背景改写：段二起点约 57% 处、其后四节，「例外」排最末 |
| 16 | 提示 | 加粗「一对」怎么数、「一句理由由实施方提炼」均未列 D | 新增 D16（加粗口径）；D8 补「措辞由实施方提炼、以 R 表『留』列为准绳」 |
| — | 保留意见 | 机制 2「每次合并前自动跑」略宽：免过闸的机械文档变更不摆卡即不跑 | 机制 2 改为「每次摆闸卡前自动跑（免过闸的机械文档变更不摆卡，也就不跑）」 |

审核未核的两组外部信源（skill-creator「yellow flag」原话、两篇 arXiv 摘要）由修订 agent 于 2026-09-26 补核，均属实；调研表对应两行改为引摘要原句。
